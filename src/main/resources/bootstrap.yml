spring:
  application:
    name: debit-card-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8095
  
management:
  endpoints:
    web:
      exposure:
        include: "*"

